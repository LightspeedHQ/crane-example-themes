# @lightspeed/crane* packages are published to the public npm registry,
# not GitHub Packages. Global ~/.npmrc routes the whole @lightspeed scope to
# npm.pkg.github.com (needed for other @lightspeed packages elsewhere), so
# override registry resolution for these specific packages here.
@lightspeed:registry=https://registry.npmjs.org/
